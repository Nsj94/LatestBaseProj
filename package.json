{
  "name": "starter-pack",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "client": "yarn workspace client start",
    "format": "prettier --write ."
  },
  "author": "Luezoid",
  "workspaces": [
    "packages/*"
  ],
  "dependencies": {
    "react-use": "^15.3.4",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "prettier": "2.7.1"
  }
}
